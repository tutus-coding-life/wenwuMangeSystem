# ===================== 核心忽略（必加） =====================
# 1. Python 虚拟环境（整个venv文件夹，无业务价值）
venv/

# 2. Python 编译缓存（自动生成，可随时重建）
__pycache__/
*.pyc
*.pyo
*.pyd
*.pyi
*.so
*.dll
*.egg-info/
.pytest_cache/

# 3. 本地数据文件夹（存储本地测试/运行数据，无需同步到远程）
data/

# ===================== 框架/工具相关 =====================
# 4. Flask-Migrate 迁移缓存（仅保留versions脚本，忽略缓存）
migrations/**/__pycache__/
# 注意：migrations/versions/ 是数据库迁移脚本，需要提交，所以只忽略其中的缓存

# 5. 环境变量文件（存储密码/密钥等敏感信息，绝对不能提交）
.env
.env.local
.env.*.local

# ===================== 开发工具/IDE =====================
# 6. VS Code 配置（如果用其他IDE，对应添加即可）
.vscode/
.vs/
# PyCharm/IntelliJ 配置
.idea/
*.iml
*.ipr
*.iws
# Sublime Text 配置
*.sublime-*

# ===================== 系统/临时文件 =====================
# 7. Windows 系统文件
Thumbs.db
Desktop.ini
$RECYCLE.BIN/
# macOS 系统文件
.DS_Store
.AppleDouble
.LSOverride
# Linux 系统文件
*.swp
*.swo
~*

# ===================== 日志/临时文件 =====================
# 8. 日志文件
*.log
logs/
# 临时文件
*.tmp
*.temp
*.bak
*.backup

# ===================== 数据库本地文件（可选） =====================
# 如果用了 SQLite 本地数据库（你的项目用MySQL，可注释）
*.db
*.sqlite3
# MySQL 本地导出的sql文件（无需提交）
*.sql
